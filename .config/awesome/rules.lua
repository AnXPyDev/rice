awful.rules.rules = {
  {
    rule = {},
    properties = {
      size_hints_honor = false,
      focus = true,
      keys = keys.client,
      buttons = buttons.client,
      screen = awful.screen.preferred
    }
  }
}
